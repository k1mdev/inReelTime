{
    "buildCommand": "cd frontend && npm install && npm build",
    "outputDirectory": "frontend/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/backend/(.*)",
            "destination": "/index.js"
        }
    ]
}